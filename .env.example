PORT=3000
JWT_SECRET=eventgate-secret-change-in-production
# Для PostgreSQL на Render/Railway — вкажи DATABASE_URL:
# DATABASE_URL=postgresql://user:pass@host:5432/eventgate
# Без DATABASE_URL використовується SQLite (файл data/eventgate.db)
